---
description: Push changes to git repository
---

This workflow commits all current changes and pushes them to the origin remote.

1. Add all changes to staging
```bash
git add .
```

2. Commit changes
// turbo
```bash
git commit -m "feat: Update portfolio UI and fix blog routing"
```

3. Push to remote
// turbo
```bash
git push origin main
```
